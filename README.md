# YTDGO-GUI: 高性能Youtube视频下载器

## About

YTDGO-GUI 是一款基于 Wails v2 和 LitElement (TypeScript) 开发的跨平台桌面视频下载工具。它集成了 yt-dlp，旨在为用户提供直观的GUI，这是个用来学习Lit的项目。

>请下载 yt-dlp 到该同一目录下。

> 需要安装 ffmpeg.

🛠️ 技术栈

    后端: Go 1.25, Wails v2 (负责系统交互、SQLite 存储、进程调度)

    前端: LitElement, TypeScript, CSS Variables (负责响应式 UI、事件监听)

    核心引擎: yt-dlp (负责视频流解析与下载)

    数据库: SQLite (用于持久化下载历史与应用设置)

✨ 核心功能

- ✅视频解析：输入单个视频 URL，自动提取不同分辨率、编码和格式，并以结构化列表展示。

- 友好的格式选择：

    - 格式分类：自动识别并标记“超清画质 (4K/8K)”、“最佳合并流”、“纯音频”。

    - 格式详情：显示推荐 ID、扩展名、分辨率及音质信息（如 1080p, 144p）。

- 批量与播放列表（施工中...）：

    - 支持多行链接批量下载。

    - 支持解析 YouTube 播放列表，支持一键“下载全部”。

- ✅实时任务管理：

    - 进度感知：实时显示百分比、下载速度和剩余时间 (ETA)。

    - 并发控制：内置下载队列，默认支持 3 个任务同时下载，其余自动排队。

- ✅下载历史：基于SQLite，自动保存已完成任务，支持直接“播放文件”、“打开文件夹”或“删除记录”。
  - ✅ 自定义下载路径

- UI/UX：

    - 支持亮色/暗黑模式切换，且在暗黑模式下具备高对比度文字优化。

    - 侧边栏徽章：实时显示正在进行的任务数量。

### 待开发特性：

✅ 多任务并行队列（用户可调并发数）

## 📖 操作指南

1. 单个视频下载

粘贴链接 -> 点击“解析” -> 在下拉菜单选择带 Emoji 标记的品质 (如 📺 1080p) -> 点击“开始下载”。

2. 播放列表解析

进入“批量任务”标签 -> 粘贴播放列表 URL -> 点击“解析播放列表” -> 预览列表后点击“下载全部”。

## ❓ 常见问题修复 (Troubleshooting)

### 历史记录无法播放？

    原因：本程序已修复该问题，在任务开始时即通过 yt-dlp --get-filename 锁定路径并存入数据库。
    
    结论：文件可能被移动。


## 编译与运行

>你需要安装wails v11.

```
# 克隆仓库 ()
git clone https://github.com/

cd YTDGO-GUI

# 编译并启动开发模式
wails dev

# 构建正式版
wails build```
```
